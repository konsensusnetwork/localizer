---
import Layout from '../layouts/Layout.astro';
---

<Layout title="BookTranslator - AI-Powered Book Translation">
  <div class="max-w-6xl mx-auto">
    <!-- Hero Section -->
    <div class="text-center mb-12">
      <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-4">
        AI-Powered Book Translation
      </h1>
      <p class="text-xl text-gray-600 mb-8 max-w-2xl mx-auto">
        Translate your books into any language using state-of-the-art AI models. 
        Secure, fast, and accurate translations powered by GPT-4, Claude, and more.
      </p>
    </div>

    <!-- React App Root -->
    <div id="app-root">
      <!-- React components will be mounted here -->
      <div class="flex justify-center items-center py-12">
        <div class="animate-spin rounded-full h-12 w-12 border-b-2 border-blue-600"></div>
      </div>
    </div>
  </div>

  <!-- Features Section -->
  <div class="mt-16 bg-white rounded-lg shadow-md p-8">
    <h2 class="text-2xl font-semibold text-center mb-8">Why Choose BookTranslator?</h2>
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <div class="text-center">
        <div class="w-12 h-12 bg-blue-100 rounded-lg flex items-center justify-center mx-auto mb-4">
          <span class="text-2xl">🤖</span>
        </div>
        <h3 class="text-lg font-medium mb-2">Multiple AI Models</h3>
        <p class="text-gray-600">Choose from GPT-4, Claude, Gemini, and more for the best translation quality.</p>
      </div>
      <div class="text-center">
        <div class="w-12 h-12 bg-green-100 rounded-lg flex items-center justify-center mx-auto mb-4">
          <span class="text-2xl">🔒</span>
        </div>
        <h3 class="text-lg font-medium mb-2">Secure & Private</h3>
        <p class="text-gray-600">Your files are encrypted and only you can access your translations.</p>
      </div>
      <div class="text-center">
        <div class="w-12 h-12 bg-purple-100 rounded-lg flex items-center justify-center mx-auto mb-4">
          <span class="text-2xl">⚡</span>
        </div>
        <h3 class="text-lg font-medium mb-2">Fast & Reliable</h3>
        <p class="text-gray-600">Get your translations quickly with real-time progress tracking.</p>
      </div>
    </div>
  </div>
</Layout>

<script>
  import '../client.tsx';
</script>

<style>
  /* Custom loading animation */
  .animate-spin {
    animation: spin 1s linear infinite;
  }
  
  @keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
  }
</style>